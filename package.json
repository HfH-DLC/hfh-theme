{
  "name": "hfh-theme",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "watch": "vite build --watch",
    "build": "vite build",
    "archive": "mkdir -p bundle && rm -rf bundle/* && dir-archiver --src . --dest ./bundle/hfh-theme.zip --exclude bundle node_modules .vscode vite.config.js .npmrc .env package.json package-lock.json",
    "bundle": "npm run build && npm run archive"
  },
  "dependencies": {
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@hfh-dlc/hfh-styleguide": "^1.0.99",
    "@vitejs/plugin-vue": "^4.0.0",
    "autoprefixer": "^10.4.13",
    "dir-archiver": "^2.1.0",
    "postcss": "^8.4.20",
    "sass": "^1.57.1",
    "vite": "^4.0.0"
  }
}
