name: Version Check

on:
  workflow_dispatch:
  pull_request:
    branches: [main]

jobs:
  call-workflow-wp-version-check:
    uses: HfH-DLC/github-workflows/.github/workflows/wp-version-check.yml@main
    with:
      wp_version_file: style.css
      use_package_json: true
